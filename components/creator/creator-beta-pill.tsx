/** Inline label for Creator Studio surfaces while the product is in public beta. */
export function CreatorBetaPill() {
  return (
    <span
      className="creator-beta-pill"
      title="Creator Studio is in beta. The writing workflow and some features are still being refined—we appreciate your patience and feedback."
    >
      Beta
    </span>
  );
}
